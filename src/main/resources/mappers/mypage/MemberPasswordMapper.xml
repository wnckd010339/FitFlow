<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.gymmanagement.mypage.mapper.MemberPasswordMapper">
    <select id="findPasswordHash" resultType="string">
        SELECT c.password_hash FROM user_local_credentials c
        JOIN users u ON u.id = c.user_id
        JOIN members m ON m.user_id = u.id
        WHERE u.id = #{userId} AND u.status = 'ACTIVE' AND m.status = 'ACTIVE'
    </select>
    <update id="updatePassword">
        UPDATE user_local_credentials
        SET password_hash = #{passwordHash}, password_changed_at = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
    </update>
</mapper>
